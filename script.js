function stringChop(str, size) {
  // your code here
	let temp=str.slice(size)
	return temp;
}

// // Do not change the code below
// const str = prompt("Enter String.");
// const size = prompt("Enter Chunk Size.");
// alert(stringChop(str, size));
